import React from 'react';
import Header from './Header.js';
import RestaurantList from './RestaurantList';

const App = () => {
    return (
        <div>
            <Header />
            <RestaurantList />
        </div>

    );
};

export default App;